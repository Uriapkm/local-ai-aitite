# Dependencias Python para Abuelo IA
# Python 3.12+ requerido

# ==========================================
# CORE - Orquestación y Agente
# ==========================================
langgraph>=0.2.0          # Orquestación de flujos agénticos (lite)
pydantic>=2.5.0           # Validación de datos y schemas
pydantic-settings>=2.1.0  # Gestión de configuración

# ==========================================
# LLM - Inferencia con Ollama
# ==========================================
ollama>=0.1.0             # Cliente oficial de Ollama para Python

# ==========================================
# AUDIO - STT y TTS
# ==========================================
faster-whisper>=1.0.0     # Speech-to-Text optimizado (CTranslate2)
sounddevice>=0.4.6        # Captura y reproducción de audio (reemplaza pyaudio)
webrtcvad>=2.0.10         # Detección de voz activa (VAD)

# TTS se ejecuta como servicio externo (Piper), solo necesitamos subprocess

# ==========================================
# VISION - Cámara y Procesamiento
# ==========================================
opencv-python-headless>=4.9.0  # Captura y procesamiento de imágenes
pillow>=10.0.0                 # Manipulación básica de imágenes

# La visión multimodal va integrada en Gemma 4 vía Ollama

# ==========================================
# HARDWARE - Arduino e IR
# ==========================================
pyserial>=3.5             # Comunicación serial con Arduino
# gpiozero ELIMINADO: Es para Raspberry Pi, no aplica en PC con Bazzite

# ==========================================
# WEB - YouTube y Navegador
# ==========================================
selenium>=4.15.0          # Automatización de Brave Browser
webdriver-manager>=4.0.0  # Gestión automática de drivers
pyautogui>=0.9.54         # Control de teclado/ratón para YouTube
python-xlib>=0.33         # Requerido por PyAutoGUI en Linux
yt-dlp>=2024.0.0          # Búsqueda y metadata de YouTube (opcional)
requests>=2.31.0          # Peticiones HTTP

# ==========================================
# MEMORIA - Base de Datos
# ==========================================
sqlite-utils>=3.36        # Utilidades para SQLite
aiosqlite>=0.19.0         # SQLite asíncrono (opcional)

# ==========================================
# UTILIDADES
# ==========================================
python-dotenv>=1.0.0      # Variables de entorno
rich>=13.0.0              # Logs y outputs con colores
tqdm>=4.66.0              # Barras de progreso
schedule>=1.2.0           # Tareas programadas (backups, etc.)

# ==========================================
# DESARROLLO Y TESTS
# ==========================================
pytest>=7.4.0             # Framework de testing
pytest-asyncio>=0.21.0    # Tests asíncronos
black>=23.0.0             # Formateo de código
isort>=5.12.0             # Ordenado de imports
flake8>=6.1.0             # Linting

# ==========================================
# OPTIMIZACIÓN AMD (RDNA2)
# ==========================================
# Nota: Las librerías de aceleración GPU (ROCm/Vulkan) 
# se instalan a nivel de sistema, no vía pip.
# Ver scripts/install.sh para detalles.

# ==========================================
# VULKAN PARA AMD RDNA2 (BC250)
# ==========================================
# Las siguientes variables se configuran en .env para optimizar Gemma 4:
# VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/radeon_icd.x86_64.json
# RADV_PERFTEST=sam,nggc
# MESA_GL_VERSION_OVERRIDE=4.6
